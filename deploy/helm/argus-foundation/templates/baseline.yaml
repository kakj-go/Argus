{{- range $key, $namespace := .Values.namespaces }}
---
apiVersion: v1
kind: LimitRange
metadata:
  name: argus-defaults
  namespace: {{ $namespace }}
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 25m
        memory: 64Mi
{{- if $.Values.network.policyApiSupported }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: argus-default-deny
  namespace: {{ $namespace }}
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: argus-allow-dns
  namespace: {{ $namespace }}
spec:
  podSelector: {}
  policyTypes: [Egress]
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: argus-allow-platform-traffic
  namespace: {{ $namespace }}
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/part-of: argus
        {{- if eq $key "system" }}
        # Domain-based exposure routes through an ingress controller that lives
        # outside the Argus namespaces; admit it for the web/session edge.
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ $.Values.network.ingressControllerNamespace | default "ingress-nginx" }}
        {{- end }}
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/part-of: argus
    - ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 6443
{{- end }}
{{- end }}
