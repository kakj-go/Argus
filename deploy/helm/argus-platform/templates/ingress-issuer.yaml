{{- if and .Values.tls.enabled (eq .Values.tls.mode "cert-manager-selfsigned") }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: argus-ingress-selfsigned
spec:
  selfSigned: {}
{{- end }}
