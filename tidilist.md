1. agent 完全测试
2. opensandbox 没有配置只是 agent 无法使用 4 个 pi 类似的工具也就是高危工具还有 stdio 也不能使用
3. card skill 功能不见了
4. argus 需要支持自定义统计界面，最好能 trace log metric 能够在同一个界面创建并展示，然后支持创建分组，分组下面可以创建多个统计界面，然后统计界面需要类似 grafana 一样支持根据 label 创建下拉过滤器，然后需要支持时间过滤+多少时间更新一次的按钮开启类似 grafana，先可以做一个 demo.html 看下，然后对应的界面创建的时候最好有个名称和描述，这个名称和描述后续可能是这样的需要让会话界面可以直接让模型根据用户语言查询对应的统计（这里可以提供一个工具）。然后可以直接让模型根据查询的结果分析其是否有问题，这个只是讨论的功能，然后可能需要提供一个工具让用户能够直接语言创建对应的统计配置，这个也是需要和主机一样是prepare+用户commit
5. argus 需要做一个告警中心，可以根据自定义告警指标，告警需要支持告警设置，告警抑制，告警合并等功能，然后后续还得考虑告警通知，通知需要对接钉钉机器人，企业微信机器人，飞书机器人，邮件这几项，然后最好创建一条告警可以设置多个告警规则也就是多个指标，然后可能还需要提供一个 tools 让会话界面的模型能够创建对应的告警和告警规则，这个也是需要和主机一样是prepare+用户commit
6. aiops